<idea-plugin url="https://plugins.jetbrains.com/author/0463a0ee-7345-44e5-84b4-a37a8d03559b">
    <id>MIFCMNotification.MIFCMNotification</id>
    <name>FCM Helper</name>
    <vendor email="opensource@mindinventory.com" url="https://github.com/Mindinventory">Mindinventory</vendor>

    <description><![CDATA[
    Plugin for the FCM notification configuration with Services and meta data for the kotlin.<br>

    If user wants to integrate FCM notification then he/she can integrate with less efforts with this plugin.<br><br>

    User will get the following features.<br>
    - Firebase service. (With notification creation) <br>
    - Service initialisation and meta data in AndroidManifest.xml <br>
    - Instruction File for the help to integrate other functionality <br><br>

    You just need to click the action of the "FCM Notification" from the New.<br><br>

    Once you will triggered the event, you can set your data and select Ok,
    Configuration, Messaging Service and meta data will add in your project.
    ]]></description>

    <change-notes><![CDATA[
    <b>v0.3</b><br/>
    - Given the support for the Android Studio's build number 192 series.<br/>
    - Given the field to update the dependency version for the messaging.<br/>
    - Updated the UI for the Panel.<br/><br/>
    <b>v0.2</b><br/>
    - Find the action with CTRL/CMD + ALT + M<br/>
    - Updated the service and instruction file in fcm directory.<br/>
    - Managed the dependency for the same module.<br/><br/>
    <b>v0.1</b><br/>
    - Updated the service and instruction file.<br/>
    ]]>
    </change-notes>

    <idea-version since-build="182.0" until-build="193.*"/>
    <depends>com.intellij.modules.lang</depends>
    <depends>org.jetbrains.kotlin</depends>
    <version>0.3.1</version>

    <extensions defaultExtensionNs="com.intellij">
        <!-- Add your extensions here -->
    </extensions>

    <actions>
        <action id="CleanPlugin.ID" class="action.SearchAction" text="FCM Notification"
                description="Configure FCM Notification service and meta data."
                use-shortcut-of="CleanPlugin.ID" keymap="$default">
            <add-to-group group-id="NewGroup" anchor="after" relative-to-action="NewDir"/>
            <keyboard-shortcut keymap="$default" first-keystroke="control alt m"/>
        </action>
    </actions>

</idea-plugin>